{% extends 'base.html.twig' %}

{% block title %}Hello FilmController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="nav">
<a href=" {{ path('show_film') }} "> Liste des Films </a><br>
<a href=" {{ path('show_realisateur') }} "> Liste des Realisateurs </a><br>
<a href=" {{ path('show_genre') }} "> Liste des Genres </a><br>
<a href=" {{ path('show_acteur') }} "> Liste des Acteurs </a><br>
<a href=" {{ path('show_personnages') }} "> Liste des personnages </a>
<hr>
</div>
<div class="example-wrapper">
    <h1>Liste des Films</h1>
<br>
<h2><a href="{{ path('add_film') }}">Ajouter un Film</a><br></h2>

{% for film in films %}
    
    <a href="{{ path('detail_film', {'id': film.id}) }}">{{film.titre}}</a> <a href="{{ path('delete_film',{'id': film.id}) }}">Supprimer</a> <a href="{{ path('update_film',{'id': film.id}) }}">Editer</a> <br>

{% endfor %}
{% endblock %}